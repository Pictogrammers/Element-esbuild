#!/usr/bin/env node
// Build
console.log('build');

// import { build } from 'esbuild';
/*
build({
  entryPoints,
  bundle: true,
  platform: 'browser',
  outfile: './dist/main.js',
  sourcemap: true,
  target: 'esnext',
  loader: {
    '.html': 'text',
    '.css': 'text'
  },
}).catch((err) => {
  process.stderr.write(err.stderr);
  process.exit(1);
});
*/